function plotSLAM(mu,S)
    q = mu(1:3);
    Sr = S (1:3,1:3);
    
    X = mu(4:2:end);
    Y = mu(5:2:end);
    
    
    %% Plot
  clf
    hold on
%     plot_cov(q,Sr,3)
    hold on
    drawFrame(q)
    
    axis equal
    drawnow
end

